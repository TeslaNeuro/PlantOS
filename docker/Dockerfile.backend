FROM python:3.12-slim

WORKDIR /app
COPY pyproject.toml README.md ./
COPY backend ./backend
COPY scenarios ./scenarios
RUN pip install --no-cache-dir -e .

EXPOSE 8000
CMD ["plantos", "serve", "--host", "0.0.0.0", "--port", "8000"]
